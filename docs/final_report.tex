\documentclass[10pt]{article}
\usepackage[margin=0.72in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{tabularx}
\usepackage{xurl}
\usepackage{hyperref}
\usepackage{enumitem}
\urlstyle{same}
\hypersetup{breaklinks=true}
\setlist{nosep,leftmargin=*}
\setlength{\parskip}{0.35em}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.18}
\setlength{\extrarowheight}{1.2pt}
\setlength{\LTpre}{0.55em}
\setlength{\LTpost}{0.55em}
\setlength{\emergencystretch}{3em}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}

\title{Team 45: Axiom Shopping Assistant\\[0.2em]\large CIS 5500 Final Report}
\author{Chenguang Shen, Luis Garcia, Leo Tan, Anika Madan}
\date{Spring 2026}

\begin{document}
\maketitle
\begin{center}
\textbf{Team Name:} Team 45 \qquad
\textbf{Application Title:} Axiom Shopping Assistant\\
\textbf{Group Members:} Chenguang Shen, Luis Garcia, Leo Tan, Anika Madan
\end{center}

\section{Introduction}
Axiom is a full-stack shopping analytics application over large Amazon product
and review data. The target problem is information overload: product pages show
prices, star ratings, review counts, and seller labels, but shoppers still have
to do the hard work of comparing alternatives, estimating whether a deal is
meaningful, and deciding which review signals are trustworthy. Axiom turns those
signals into searchable product discovery, product-detail evidence, cart
savings, category and brand analytics, and a customizable value ranking.

The application was built by Chenguang Shen, Luis Garcia, Leo Tan, and Anika
Madan. Our main goals were:
\begin{itemize}
  \item integrate overlapping product, category, brand, user, and review/comment
  data into a normalized PostgreSQL schema;
  \item expose the data through robust Express API routes with input validation
  and stable JSON response shapes;
  \item build a React/Vite frontend with multiple distinct pages that all read
  from the database-backed API; and
  \item preserve original and optimized SQL paths so performance gains from
  indexing, caching, query restructuring, and materialized views are measurable.
\end{itemize}

The final interface includes a visible query-mode toggle. In standard mode the
client calls original SQL paths; in optimized mode it appends
\texttt{?optimized=1}, letting the backend choose optimized SQL or cached
responses without changing the public API.

\section{Architecture}
The system has three tiers. The frontend is a React/Vite single-page
application, the backend is an Express server using the Node \texttt{pg} pool,
and the database is PostgreSQL hosted on AWS RDS. The data pipeline is written
in Python with pandas and psycopg2; operational SQL and benchmark scripts live
under \texttt{database/} and \texttt{scripts/}.

\begin{center}
\begin{tabularx}{\linewidth}{@{}L{0.16\linewidth}L{0.27\linewidth}Y@{}}
\toprule
Layer & Technologies & Responsibility \\
\midrule
Client & React, Vite, React Router & Pages, filters, charts, cart state \\
API & Node.js, Express, \texttt{pg} & Validation, caching, route contracts \\
Database & PostgreSQL on AWS RDS & Normalized tables, indexes, materialized views \\
Data tools & Python, pandas, psycopg2 & Cleaning, entity resolution, ingestion \\
Testing/ops & Vitest, Supertest, benchmark scripts & Regression tests and timings \\
\bottomrule
\end{tabularx}
\end{center}

Browser pages call a small client API wrapper, which appends the global
\texttt{optimized=1} flag when the user chooses optimized query mode. Express
route modules validate request input, select the original or optimized SQL
variant, query PostgreSQL through a shared pool, and return stable JSON response
shapes. Materialized views and indexes live in the database layer, while the
frontend remains unaware of whether a response came from an original SQL query,
an optimized SQL query, or a short-lived cache.

\section{Data}
The final database integrates product metadata with two review/comment sources:
\begin{itemize}
  \item \textbf{Amazon Products 2023.} We use the Amazon product and category
  catalog released on Kaggle by asaniczka:
  \href{https://www.kaggle.com/datasets/asaniczka/amazon-products-dataset-2023-1-4m-products}{Amazon Products Dataset 2023}. It provides ASINs, titles, image URLs,
  product URLs, prices, list prices, star ratings, review counts, best-seller
  flags, and category IDs.
  \item \textbf{Original Amazon review/comment export.} The checked-in
  \texttt{data/raw/amazon\_reviews.csv} comes from the
  \href{https://amazon-reviews-2023.github.io/main.html}{Amazon Reviews'23}
  review/comment release and has 701,528 raw comment rows with ratings, review
  titles, review text, helpful votes, verified-purchase flags, timestamps,
  ASINs, parent ASINs, and reviewer IDs. This source shaped the product-detail
  review schema and supplies text/helpfulness fields when linked rows are
  present.
  \item \textbf{McAuley Lab Amazon Reviews 2023, Pure IDs 5-Core.} To build a
  high-volume interaction table, we also used the full 2023 McAuley Lab Amazon
  Reviews dataset, specifically the
  \href{https://amazon-reviews-2023.github.io/data_processing/5core.html}{Pure IDs
  (5-Core)} files from the
  \href{https://amazon-reviews-2023.github.io/main.html}{Amazon Reviews'23}
  release. These files provide dense user-product rating and timestamp signals.
  Pure-ID rows do not always contain free-text, helpful-vote, or verified fields,
  so the cleaner safely defaults those optional columns while preserving the
  rating, user, product, and time dimensions used by analytics.
\end{itemize}

The product and review datasets overlap through ASIN and parent-ASIN identifiers.
That overlap is what lets Axiom answer questions that require both sources, such
as ``Which discounted products also have strong recent review activity?'' and
``Which brands have high product ratings and strong linked-review evidence?''

In the application, product metadata drives search, deal cards, category/brand
pages, and price comparisons. Review rows supply recent-activity signals,
review histograms, reviewer credibility context, and analytics aggregates.
Extracted brand and category entities make it possible to compare performance at
a level above individual products.

\subsection{Cleaning and Summary Statistics}
The cleaner deduplicates products by ASIN, parses numeric prices and ratings,
normalizes category foreign keys, extracts canonical brand entities from product
titles, and drops brands with fewer than five products to \texttt{NULL
brand\_id}. Brand extraction is intentionally conservative because the product
source has no official brand column.

For reviews, the pipeline streams CSV chunks so it can process multi-million-row
inputs. It normalizes headers across the comment and Pure-ID sources, requires
\texttt{user\_id} and a valid 1-5 rating, accepts either \texttt{asin} or
\texttt{parent\_asin}, falls back to \texttt{parent\_asin} when the child ASIN is
not in the product catalog, filters orphan reviews, parses both text timestamps
and millisecond epochs, clamps helpful votes to non-negative integers, and
deduplicates review facts by \texttt{(asin, user\_id, review\_timestamp)}.

\begin{center}
\begin{tabularx}{\linewidth}{@{}L{0.14\linewidth}rL{0.24\linewidth}Y@{}}
\toprule
Relation & Rows & Key statistic & Notes \\
\midrule
Categories & 248 & 248 linked categories & Original IDs preserved \\
Brands & 26,484 & 26,484 linked brands & Extracted from product titles \\
Products & 1,426,336 & Avg. price \$43.38 & Median price \$19.95, avg. stars 4.00 \\
Users & 1,279,603 & Distinct reviewers & From linked review rows \\
Reviews & 2,998,677 & Avg. rating 4.392 & Timestamps from 1999-11-05 to 2023-09-11 \\
\bottomrule
\end{tabularx}
\end{center}

The final cleaned review snapshot is larger than the checked-in
\texttt{amazon\_reviews.csv} because the full Pure IDs 5-Core files were used
during final ingestion. Those full files are not included in the submission
archive because of size, but the cleaning code supports the same workflow by
discovering additional review streams in \texttt{data/raw/}.

\section{Database}
The ingestion script applies \texttt{database/schema.sql}, loads cleaned CSVs
with PostgreSQL \texttt{COPY}, validates foreign-key relationships, and grants a
read-only guest user. Entity resolution happens before loading: products are
deduplicated by ASIN, reviews are linked through ASIN or parent ASIN, brands are
canonicalized from product titles, and orphan review rows are removed. The
normalized base schema has five relations:

\begin{center}
\begin{tabularx}{\linewidth}{@{}L{0.14\linewidth}rL{0.16\linewidth}Y@{}}
\toprule
Table & Rows & Key & Main attributes \\
\midrule
Categories & 248 & \texttt{category\_id} & \texttt{category\_name} \\
Brands & 26,484 & \texttt{brand\_id} & \texttt{brand\_name} \\
Products & 1,426,336 & \texttt{asin} & Title, URLs, prices, stars, counts, category/brand FKs \\
Users & 1,279,603 & \texttt{user\_id} & Reviewer identifier \\
Reviews & 2,998,677 & \texttt{review\_id} & ASIN/user FKs, rating, text, helpfulness, timestamp \\
\bottomrule
\end{tabularx}
\end{center}

\begin{center}
\includegraphics[width=0.92\linewidth]{er_diagram.png}
\end{center}

\subsection{3NF Proof}
\textbf{Categories.} \texttt{category\_id -> category\_name}; the determinant is
the primary key, and \texttt{category\_name} is unique.

\textbf{Brands.} \texttt{brand\_id -> brand\_name}; the determinant is the
primary key, and \texttt{brand\_name} is unique after canonicalization.

\textbf{Products.} ASIN determines title, image URL, product URL, price, list
price, stars, review count, best-seller flag, category ID, and brand ID. ASIN is
the primary key. Category and brand names are not stored redundantly on Products,
so there is no transitive dependency from ASIN through a foreign key to a
non-key display name.

\textbf{Users.} The only stored attribute is \texttt{user\_id}, which is the
primary key. There are no non-key attributes.

\textbf{Reviews.} Review ID determines the linked ASIN, user ID, rating, title,
text, helpful-vote count, verified-purchase flag, and timestamp. Product and
user details remain in Products and Users. The review fact table stores the
relationship and the review attributes only.

Every non-trivial functional dependency in the base schema has a superkey on the
left or a prime-attribute exception that does not arise here, so the design is
in 3NF. The materialized views used for optimization are derived read models,
not replacement base relations.

\section{Web App Description}
The React frontend is a single-page application with pages that exercise
different database-backed workflows. Each page uses the shared API client, so
the global query-mode toggle can switch between original and optimized backend
paths without changing page logic.

\small
\begin{longtable}{@{}L{0.15\linewidth}L{0.28\linewidth}L{0.48\linewidth}@{}}
\toprule
Page & Main API routes & Distinct functionality \\
\midrule
\endhead
Home &
\path{/products/search}, \path{/products/trending}, \path{/deals} &
Entry point for search plus quick product discovery through proven picks and
discounted items. It is optimized for fast first impressions rather than deep
filtering. \\
Browse &
\path{/categories}, \path{/products/search}, \path{/products/category} &
Combines keyword search with category exploration and URL-backed filters. It
also links into full category pages when users want more than preview cards. \\
Deals &
\path{/deals} &
Focuses on discount ranking, price caps, and star filters. Unlike search, the
primary sort is deal quality rather than title relevance. \\
Category and Brand &
\path{/products/category}, \path{/products/brand}, \path{/categories} &
Paginated product-list pages for one category or one brand, with rating and
price filters preserved in the URL. These were added after Milestone 4 because
the final UI needed stable browse destinations. \\
Product Detail &
\path{/products/:asin}, \path{/products/:asin/rating-distribution},
\path{/products/:asin/helpful-reviews}, \path{/products/:asin/alternatives} &
Shows one product with review evidence, reviewer context, cheaper higher-rated
alternatives, and cart actions. This page joins product metadata with review
analytics. \\
Cart &
\path{/cart/savings} &
Persists selected ASINs in local storage and asks the backend to aggregate list
price, current price, and savings for the current cart. \\
Analytics &
\path{/analytics/categories/compare}, \path{/analytics/brands/performance},
\path{/analytics/reviews/trend} &
Visualizes category-level aggregates, brand-level review evidence, and monthly
review trends. It is the most dashboard-like page and emphasizes comparison. \\
Value Rankings &
\path{/products/value-rankings} &
Lets users weight rating strength, review depth, price efficiency, and recent
activity. The Balanced preset uses \texttt{0.4/0.2/0.2/0.2}, matching the
indexed default score in the optimized materialized view. \\
\bottomrule
\end{longtable}
\normalsize

\section{API Specification}
The Milestone 4 API Specification in \path{docs/reference/api_spec.pdf}
documented 15 routes: 12 query-backed primary routes and 3 auxiliary UI routes.
The final backend preserves those contracts and executes 17 public routes. The
two public routes added after Milestone 4 are \path{/products/category} and
\path{/products/brand}; they support the final Browse, Category, and Brand
pages. Routes accept an optional \texttt{optimized=1} or
\texttt{optimized=true} query parameter when an optimized branch exists.

\footnotesize
\begin{longtable}{@{}L{0.18\linewidth}L{0.26\linewidth}L{0.27\linewidth}L{0.23\linewidth}@{}}
\toprule
Method and path & Functionality & Request parameters & Response parameters \\
\midrule
\endhead
GET \path{/categories} &
M4 auxiliary route. Lists categories for Browse and Analytics selectors. &
Query: \texttt{optimized} bool optional. &
\path{category_id} int; \path{category_name} string. \\
GET \path{/brands} &
M4 auxiliary route. Lists extracted brands for filters and links. &
Query: \texttt{optimized} bool optional. &
\path{brand_id} int; \path{brand_name} string. \\
GET \path{/products/:asin} &
M4 auxiliary route. Loads one product before detail-side analytics run. &
Path: \texttt{asin} string. Query: \texttt{optimized} bool optional. &
\path{asin}, \path{title}, image/product URLs as strings; \path{price},
\path{list_price}, \path{stars} as floats; \path{review_count} int;
\path{is_best_seller} bool; category/brand names as strings. \\
GET \path{/products/search} &
M4 query route. Searches product titles and filters by minimum rating. &
Query: \texttt{keyword} string required; \texttt{minStars} float optional;
\texttt{limit}, \texttt{offset} int optional. &
Product rows with ASIN/title/image URL strings, price and stars floats,
review count int, and best-seller bool. \\
GET \path{/deals} &
M4 query route. Ranks discounted products for the Deals page. &
Query: \texttt{maxPrice} float optional; \texttt{minStars} float optional;
\texttt{limit}, \texttt{offset} int optional. &
Product rows with ASIN/title strings, list/current price floats,
\path{discount_pct} float, stars float, review count int, and category string. \\
GET \path{/products/category} &
Post-M4 route. Returns paginated products for one category page. &
Query: \texttt{category} string required; \texttt{minStars}, \texttt{maxPrice}
optional; \texttt{limit}, \texttt{offset} int optional. &
Product-card rows with ASIN/title strings, price/list price/discount/stars
floats, review count int, best-seller bool, and category/brand strings. \\
GET \path{/products/brand} &
Post-M4 route. Returns paginated products for one brand page. &
Query: \texttt{brand} string required; \texttt{minStars}, \texttt{maxPrice}
optional; \texttt{limit}, \texttt{offset} int optional. &
Product-card rows with ASIN/title strings, price/list price/discount/stars
floats, review count int, best-seller bool, and category/brand strings. \\
GET \path{/products/:asin/rating-distribution} &
M4 query route. Builds the product-detail rating histogram. &
Path: \texttt{asin} string. Query: \texttt{optimized} bool optional. &
\path{rating} float; \path{review_count} int; \path{verified_ratio} float. \\
GET \path{/products/:asin/helpful-reviews} &
M4 query route. Shows top reviews with reviewer credibility context. &
Path: \texttt{asin} string. Query: \texttt{optimized} bool optional. &
\path{review_id} int; rating float; title/text strings; helpful votes int;
verified bool; timestamp; reviewer total reviews int and average rating float. \\
GET \path{/products/:asin/alternatives} &
M4 query route. Finds cheaper, higher-rated products in the same category. &
Path: \texttt{asin} string. Query: \texttt{optimized} bool optional. &
Alternative rows with ASIN/title/image URL strings, price/stars floats, review
count int, and category string. \\
POST \path{/cart/savings} &
M4 query route. Aggregates list price, current price, and savings for a cart. &
Body: \texttt{asins} array of strings, maximum 200. Query:
\texttt{optimized} bool optional. &
\path{total_list_price}, \path{total_current_price}, and \path{total_savings}
as floats. \\
GET \path{/analytics/categories/compare} &
M4 query route. Computes category-level price, rating, and count metrics. &
Query: \texttt{optimized} bool optional. &
\path{category_name} string; \path{avg_price}, \path{avg_rating} floats;
\path{product_count} int. \\
GET \path{/products/trending} &
M4 query route. Ranks category products by recent linked review activity. &
Query: \texttt{category} string required; \texttt{months} int optional;
\texttt{optimized} bool optional for the default window. &
Product rows with ASIN/title/image URL strings, price/stars floats, review count
int, \path{recent_review_count} int, best-seller bool, and brand string. \\
GET \path{/products/top-value} &
M4 query route. Finds below-category-average price and above-average rating
products with recent reviews. &
Query: \texttt{reviewedSince} ISO date optional; \texttt{optimized} bool
optional. &
ASIN/title/image URL strings, price/stars floats, review count int, and category
string. \\
GET \path{/analytics/brands/performance} &
M4 query route. Computes brand-level product and review aggregates. &
Query: \texttt{optimized} bool optional. &
\path{brand_name} string; product rating floats; product/review counts ints;
verified review count int; average review score float; helpful-vote total int. \\
GET \path{/analytics/reviews/trend} &
M4 query route. Groups monthly category reviews by reviewer credibility group. &
Query: \texttt{category} string required; \texttt{optimized} bool optional. &
\path{review_month} timestamp; total reviews int; overall, high-, and
low-credibility average ratings as floats; credibility gap float; verified count
int. \\
GET \path{/products/value-rankings} &
M4 query route. Builds a weighted normalized product value score. &
Query: \texttt{wRating}, \texttt{wReviews}, \texttt{wPriceEff},
\texttt{wRecent} floats optional; \texttt{optimized} bool optional. &
Product rows with ASIN/title strings, price/stars floats, review counts ints,
category/brand strings, and \path{value_score} float. \\
\bottomrule
\end{longtable}
\normalsize

\section{Queries}
The application uses query-backed routes throughout the frontend. The examples
below are the most complex query families, and each one is exercised by a
visible page or widget in the web app. The final implementation also includes
\texttt{*QueryOptimized} exports and materialized-view-backed SQL for
\texttt{?optimized=1}; these are post-Milestone-4 implementation additions, not
new public endpoints. The internal \texttt{productExistsQuery} in
\path{server/queries/products/search.sql.js} supports clean 404 responses before
product-specific subqueries run.

\small
\begin{longtable}{@{}L{0.18\linewidth}L{0.23\linewidth}L{0.34\linewidth}L{0.19\linewidth}@{}}
\toprule
Route and page & Source file & How it is used & Why it is complex \\
\midrule
\endhead
\path{/analytics/brands/performance} on Analytics &
\path{server/queries/analytics.sql.js} &
Ranks brands by product rating, linked-review volume, verified-review evidence,
average review score, and helpful votes. &
Brands/Products/Reviews joins, CTEs, grouping, filters, and ordered aggregates. \\
\path{/analytics/reviews/trend} on Analytics &
\path{server/queries/analytics.sql.js} &
Shows monthly review trends for a selected category and compares high-activity
and low-activity reviewer groups. &
Reviewer-count CTE, category joins, \texttt{DATE\_TRUNC}, conditional
aggregation. \\
\path{/products/trending} on Home/Browse &
\path{server/queries/products/trending.sql.js} &
Ranks products in one category by recent review activity, then uses rating and
review count as tie-breakers. &
Review-horizon CTE, aggregation by ASIN, category/product/brand joins. \\
\path{/products/top-value} on Home/Value flows &
\path{server/queries/products/value.sql.js} &
Finds products cheaper than their category average, rated above their category
average, and reviewed since the requested date. &
Correlated subqueries, EXISTS check, category/product joins, top-k ordering. \\
\path{/products/value-rankings} on Value Rankings &
\path{server/queries/products/value.sql.js} &
Combines rating, review count, price efficiency, and recent activity using
user-selected weights. &
Multiple CTEs, global bounds, category bounds, normalization, score sorting. \\
\path{/products/:asin/helpful-reviews} on Product Detail &
\path{server/queries/products/detail.sql.js} &
Returns the target product's most helpful reviews with reviewer-level context. &
Reviewer aggregate CTE in original path; optimized lateral stats over top
reviews. \\
\path{/deals} on Deals/Home &
\path{server/queries/products/deals.sql.js} &
Ranks products by discount percentage while applying price and rating filters. &
Computed expression sort, filtered product scan, category join. \\
\path{/products/category} and \path{/products/brand} on Browse pages &
\path{server/queries/products/browse.sql.js} &
Provides paginated category and brand product lists with filters and display
names. &
Product/category/brand joins, pagination, filter predicates, optimized CTEs that
page products before joining display names. \\
\bottomrule
\end{longtable}
\normalsize

\section{Performance Evaluation}
The benchmark runner executes one discarded warmup run, pauses for one second,
then records five measured \texttt{EXPLAIN ANALYZE} runs. The table reports
median execution time from the latest \texttt{docs/timings.md}. Representative
inputs are selected from the loaded database: the category with the most linked
reviews (\texttt{Toys \& Games}), an ASIN with more than 50 reviews
(\texttt{B0178IC734}), \texttt{reviewedSince=2018-01-01} for top-value, and the
Balanced \texttt{0.4/0.2/0.2/0.2} value-ranking weights. The events being timed
are backend SQL execution plans for representative route queries, not React
rendering time or HTTP network latency.

\begin{center}
\small
\begin{tabularx}{\linewidth}{@{}L{0.17\linewidth}rrrY@{}}
\toprule
Query & Orig. ms & Opt. ms & Speedup & Main optimization \\
\midrule
Deals & 0.5 & 0.5 & 1.00x & Discount expression index already applied in current run \\
Category comparison & 3113.5 & 0.4 & 7962.92x & Materialized category summary \\
Top value & 324918.8 & 855.3 & 379.87x & Precomputed value components + MV index \\
Brand performance & 2366.2 & 3.7 & 643.68x & Materialized brand summary + sort index \\
Value rankings & 17047.1 & 1.4 & 12133.17x & Indexed default score in value MV \\
Trending & 1904.6 & 9.7 & 195.98x & Indexed recent-review count in value MV \\
Helpful reviews & 9118.3 & 7.1 & 1290.44x & Rank target reviews before reviewer stats \\
Review trend & 4601.9 & 4853.4 & 0.95x & Same SQL; route cache helps repeated reads \\
\bottomrule
\end{tabularx}
\normalsize
\end{center}

\subsection{Optimization Techniques}
\textbf{Indexing.} The schema includes foreign-key indexes, review timestamp and
ASIN indexes, category/brand product ordering indexes, a trigram index for title
search, and specialized materialized-view indexes such as
\path{idx_mv_value_components_default_score_cover}. A partial expression index
on \texttt{(1 - price/list\_price)} for valid discounted products keeps the
deals route sub-millisecond in the latest benchmark, while the largest measured
speedups now come from materialized category, brand, and value-summary paths.

\textbf{Materialized views.} \texttt{mv\_category\_compare},
\texttt{mv\_brand\_performance}, and \texttt{mv\_value\_score\_components}
precompute expensive aggregates and normalized dimensions. The value view stores
category price/rating averages, recent review counts, latest review timestamps,
normalized score components, and an indexed default Balanced score.

\textbf{Query restructuring.} The optimized helpful-review query first ranks the
target product's top ten reviews, then computes reviewer stats only for those
users. Category and brand listing queries page product rows before attaching
display names. Value ranking splits the default weighted case from custom
weights so the common Balanced preset can use the indexed precomputed score.

\textbf{Caching.} The backend uses a small in-memory TTL cache for optimized
category/brand metadata, optimized category comparison, and optimized review
trend by category. Brand performance is already backed by a small materialized
view and is returned without route-level caching to keep the code path simple.

\textbf{Why some optimizations are modest.} Some routes are already primary-key
or small-table lookups, so optimized and original timings are nearly identical.
The latest deals benchmark is also sub-millisecond in both branches because the
discount index benefits the current route shape. Review trend remains close
because the grouped SQL is already selective for the representative category;
route caching improves repeat users rather than the first
\texttt{EXPLAIN ANALYZE} timing.

\section{Technical Challenges}
\textbf{Product/review entity alignment.} The largest data challenge was joining
product ASINs with heterogeneous review sources. Some comment rows reference a
child ASIN, some use \texttt{parent\_asin}, and many raw reviews do not appear
in the product catalog. The cleaner handles both IDs, falls back to parent ASIN
when appropriate, and removes orphan reviews before ingestion.

\textbf{Brand entity resolution.} Product titles are noisy and the catalog has
no brand column. We used conservative title-prefix extraction, punctuation and
legal-suffix normalization, canonical display names, and a minimum five-product
threshold to avoid creating thousands of one-off brand entities.

\textbf{Large-file processing.} The full review sources are too large to clean
comfortably in memory. The pipeline streams review chunks, writes the review CSV
incrementally, and tracks users and duplicate keys while preserving stable
foreign-key relationships.

\textbf{Optimization tradeoffs.} Some rewritten SQL shapes were slower than the
planner's original choices. We therefore kept original variants for comparison,
used materialized views for aggregates that are stable between refreshes, and
only cached routes where repeat calls are common and the response changes
infrequently.

\textbf{Validation and regression coverage.} The API validates malformed ASINs,
missing required query parameters, invalid numeric ranges, invalid dates,
zero-sum ranking weights, and oversized cart arrays. SQL uses parameterized
bindings throughout. Server tests mock the database pool and cover validation,
query selection, caching, and response shape; client tests cover API path
building, top-level routing including dynamic category, brand, and product
routes, cart behavior, pages, charts, formatting helpers, and the query-mode
toggle.

\section*{Extra Credit and AI Tool Use}
Deployment extra credit is claimed for the hosted application configuration. The
frontend is configured with \texttt{VITE\_API\_BASE\_URL} set to
\url{https://team45-cis550-final-project.vercel.app}, and the backend CORS
allowlist is configured with \texttt{CLIENT\_ORIGIN} set to
\url{https://cis550-final-project.onrender.com}. The project also includes
substantial automated testing for robustness, and coverage can be measured with
the root and client coverage scripts.

Per the course plagiarism policy, generative AI was used to help with the
frontend's visual design direction and during final documentation and
consistency review to identify drift between the code, rubric, and writeups. The
team remains responsible for the project design, implementation, verification,
and final submitted content.

\end{document}
