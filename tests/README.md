# Root Test Reservation

This directory is reserved for future cross-cutting integration or E2E tests.

Backend unit and route tests now live under `server/tests/`, and frontend tests
continue to live under `client/tests/`.
